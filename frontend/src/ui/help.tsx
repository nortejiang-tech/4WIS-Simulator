/**
 * help.tsx — central registry of per-module usage instructions.
 *
 * Each entry is a small JSX snippet rendered inside <Panel>'s help popover when
 * the user clicks the ⓘ button. Keep them short and action-oriented — they are
 * the in-app counterpart to the HTML 功能说明书.
 */

import type { ReactNode } from "react";

export const HELP: Record<string, ReactNode> = {
  // ── 驾驶 ──────────────────────────────────────────────
  model: (
    <>
      三档保真度,从快到真:<b>运动学</b>(纯几何,曲线最干净)·<b>简化动力学</b>
      (线性轮胎+侧偏,看横摆/侧偏角)·<b>多体 14DOF</b>(载荷转移/侧倾,唯一体现齿条力链)。
    </>
  ),
  strategy: (
    <>
      选择「方向盘→四轮转角」的分配方式。<b>后轮转向</b> 是一个策略、下方下拉选 5 种控制方法
      （定比 / 车速调度 / 横摆反馈 / 稳态+瞬态 / 模型跟踪）。<b>user_js</b> 接收浏览器侧指令——
      <b>策略设计器和 JS 沙箱都靠它驱动</b>,要用设计器请先选它。
    </>
  ),
  driver: (
    <>
      实时回读当前油门/方向盘。<b>油门=目标速度比例</b>(<code>throttle×v_max=目标车速</code>),
      不是加速踏板。键盘 <b>W/S</b> 油门、<b>A/D</b> 转向、<b>空格</b> 松油门、<b>R</b> 复位。
      <br /><b>定速巡航</b>:勾选并填车速(km/h),锁定车速但转向仍可控——研究转向时车速保持干净常量。
    </>
  ),
  friction: (
    <>
      选路面预设(水泥/沥青/雨/雪/冰)设基础附着系数 μ,再用滑块微调。
      μ 越低越易打滑;split-μ 等局部扰动在「场景」页单独设置。
    </>
  ),
  vehicleQuick: <>常用整车参数速查/微调。完整几何/悬架/轮胎参数在「场景」页的「车辆 / 悬架参数」。</>,
  simControl: <>暂停/继续/复位仿真。复位会把车辆移回原点并清空轨迹。</>,

  // ── 设计 ──────────────────────────────────────────────
  designer: (
    <>
      所见即所得设计四轮转向分配,<b>无需写代码</b>。
      <br />★ <b>必须先把策略切到 <code>user_js</code></b> 才会驱动仿真。
      <ol>
        <li><b>曲线</b>模式:每轮一条曲线(X=方向盘 −1…1,Y=轮角 ±60°);拖点 / 点空白加点 / 右键删点。</li>
        <li><b>ICR</b>模式:直接拖瞬心,四轮转角按几何实时反解;双击复位为直行。</li>
        <li><b>k(vx) 调度</b>模式:画后轮/前轮比随车速的曲线(低速反相/高速同相),
          点右上「切到车速调度」用 <code>rws_speed_schedule</code> 策略生效。</li>
        <li>选预设(阿克曼/蟹行/后轮反向/零半径)打底,用 A/D 试,去「验证」页看评分。</li>
      </ol>
    </>
  ),
  trajectory: (
    <>
      生成标准路径(直线/圆弧/绕桩/双移线/八字/停车)或点击画布放航点;配合
      <code>轨迹跟踪</code> 策略做 pure-pursuit 自动循迹。可设跟踪定速车速。
    </>
  ),
  userPython: (
    <>
      编辑 <code>plugins/strategies/user_strategy.py</code> 保存后 ~1s 自动热重载,无需重启。
      接口 <code>compute(driver, state) → {"{'delta_cmd':[…]}"}</code>;选策略 <code>user_py</code> 生效。
    </>
  ),
  jsStrategy: (
    <>
      直接在浏览器写 <code>compute(driver, state)</code>,实时编译执行、经 WebSocket 回传(后端零 eval)。
      选策略 <code>user_js</code> 生效。<br />⚠ 与策略设计器共用 user_js 通道,<b>一次只用一个</b>。
    </>
  ),

  // ── 验证 ──────────────────────────────────────────────
  excitation: (
    <>
      一键跑标准化驾驶员输入(可复现):<b>角阶跃</b>(瞬态)·<b>单频正弦</b>·
      <b>正弦扫频</b>(带宽)·<b>双移线 ISO-3888</b>(换道)。
      <ol>
        <li>选工况,调幅值/频率/时长。</li>
        <li>设<b>目标车速</b>(km/h),油门按它定速保持。</li>
        <li>需要干净对比就勾「运行前清空」,点运行(60Hz,切后台也不停)。</li>
      </ol>
    </>
  ),
  score: (
    <>
      从历史实时算 7 项指标(瞬心偏差峰值/RMS、横摆、侧向速度、能耗、齿条力、侧偏角)。
      <br />跑一次 → 「捕获为 A」→ 换策略再跑 → 「捕获为 B」,面板<b>并排对比并高亮更优方</b>。
    </>
  ),
  compare: (
    <>
      叠加两次行驶轨迹(A 绿/B 橙)并对比曲线,可切换通道:车速/横摆角速度/瞬心偏差/电机力矩。
      在「数据」页录制后或评分捕获后使用。
    </>
  ),

  // ── 场景 ──────────────────────────────────────────────
  params: (
    <>
      完整编辑几何/质量惯量/转向伺服/悬架/弹簧阻尼/轮胎/力链传动参数,应用即重建模型生效;
      非法值会显示后端校验详情。<b>车速类字段以 km/h 显示</b>。
    </>
  ),
  disturbance: (
    <>
      画布编辑路面扰动:选类型→「放置」→点画布连续放置(Esc 退出);点选已有区域可拖动/改参/删除。
      类型含 减速带/斜坡/split-μ/冰面。
    </>
  ),
  fault: (
    <>
      故障注入(功能安全验证):执行器(电机卡死/限幅,改实际转角)+ 传感器(偏置/噪声/丢帧,只改上报值)。
      增删改+启停,用于验证某轮失效时整车可控性与策略容错。
    </>
  ),
  project: <>保存/加载 YAML 项目(整车+场景+控制器一并持久化)。改 <code>projects/*.yaml</code> 也可。</>,
  scenario: (
    <>
      选一个标准场景给手动驾驶提供场景感：<b>广场</b>(开阔)·<b>小镇·士瓦本格明德</b>
      (仿德国老城:椭圆环路 + 密集内街网 + 集市/教堂广场 + 沿街建筑 + 小河,3D 建筑立体)·
      <b>小赛道</b>·<b>上海国际赛车场(近似)</b>。加载后车辆移到场景起点,2D/3D 同步渲染;「清除场景」回到空网格。
      <br />场景为静态装饰(只渲染一次),不影响帧率;路面摩擦/扰动仍在本页其它面板单独设置。
    </>
  ),

  // ── 数据 ──────────────────────────────────────────────
  measure: (
    <>
      <b>卷尺</b>:点「开始测量」,到 2D 画布点两点→黄虚线+距离;第三次点重新开始。
      <br /><b>轨迹自动尺寸</b>:实时显示 X/Y 跨度、路径总长、首尾直线位移。
      <br />⚠ 与画布放置扰动/航点互斥。
    </>
  ),
  recording: <>整段录制 CSV(含 ICR、力链 8 通道、侧偏角等)。开始→行驶→停止→导出。</>,
  script: <>加载 YAML 动作脚本(<code>scripts_lib/*.yaml</code>)自动执行预设工况序列。</>,
  chart: (
    <>
      实时曲线:车速(km/h)+横摆、瞬心偏差、齿条力/电机力矩(仅多体)、侧偏角(仅动力学)。
      每张可导出 PNG/CSV(当前 30s 窗口)。
    </>
  ),
};
